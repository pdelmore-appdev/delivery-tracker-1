<!DOCTYPE html>
<html>
  <head>
    <title>Delivery Tracker 1</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

<style>
.flash {
  padding: 10px;
}


</style>

  </head>

  <body>

 <% if @current_user == nil %> 
<div>
  <a href="/user_sign_in">Sign in</a> | <a href="user_sign_up">Sign up</a>
</div>
<% else %>
<div>
  <a href="/">Your todo list</a> | <a href="edit_user_profile">Edit user profile</a> | <a href="user_sign_out">Sign out</a>
</div>
<% end %>

<div class="flash">
<%= notice %>
<%= alert %>
</div>

    <%= yield %>
  </body>
</html>
